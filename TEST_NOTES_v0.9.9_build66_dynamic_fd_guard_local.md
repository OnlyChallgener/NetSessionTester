# NetSessionTester v0.9.9 build66 local

本地测试版，不用于推送更新。

## 调整
- FD 保护从固定 32000/32600 改为动态读取 `/proc/self/fd` 与 `/proc/self/limits`。
- 剩余 FD <= 900：进入预警，降低 CPS。
- 剩余 FD <= 500：进入保护，进入顶部确认，仅少量补测。
- 剩余 FD <= 300：硬停并释放。
- 活动连接 32600 仅作为兜底保护，避免 FD 读取异常时继续冲顶。
- 保留 build65 的忽略更新和底部栏适配。
