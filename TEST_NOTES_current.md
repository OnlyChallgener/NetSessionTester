# 当前自测重点 build130

1. Ping 目标输入错误域名、错误 URL、空地址，确认不会开始测试。
2. 粘贴 https://example.com/path?x=1，确认自动提取 example.com。
3. 输入 host:port，连接数测试端口应跟随 URL/输入端口。
4. 普通 Ping 运行中切后台/锁屏，确认测试中断、状态变为已中断，不再画直线。
5. 重新进入 APP 后检查 Ping 历史是否保存部分记录并标记中断。
6. 连接数测试启动时，Ping 目标仍同步连接数测试地址。
