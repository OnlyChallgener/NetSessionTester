# 当前多平台分支测试说明 (v1.0.22-beta1 build157)

本轮完成桌面端 (Windows/macOS) 4-Tab 导航重构、动态自适应图表缩放与平移、光标吸附 Tooltip、8 指标仪表盘网格及检查更新弹窗；完善 iOS 核心工具迁移与原生 Liquid Glass 标签栏接入；优化日志机制与并发释放。

历史版本的构建清单已归档至 [docs/BUILD_HISTORY.md](docs/BUILD_HISTORY.md)，未来发版模板请使用 [docs/RELEASE_TEMPLATE.md](docs/RELEASE_TEMPLATE.md)。

---

## 本地契约与自动化检查

本地 43 项 Python 源契约、协议样例、生命周期与配置检查通过：
```bash
python -m unittest discover -s docs/validation -p 'test_*.py' -v
```

优先实机检查关注点：
1. 连续测试起始图表曲线清空并重绘，不复用上一轮残余点；
2. Ping 失败时（超时/拒绝）不显示假 0ms 或继承前序有效延迟；
3. 停止与释放后活动套接字清零，对端主动断开被正确识别；
4. 移动端/桌面端页面切换或进入后台时，后台探测及时终止并释放资源；
5. 系统标签栏与窗口边框自适应良好，无内容遮挡或透图。

---

## v1.0.22-beta1 build157 核心验证清单

### 1. 桌面端 4-Tab 顶部导航与界面布局 (Windows / macOS)
- [ ] 顶部导航统一为 4 个 Tab：「并发压测」、「独立 Ping」、「联动诊断」、「测试历史」，点击平滑切换无卡顿；
- [ ] 切换 Tab 时，若有正在运行的测试任务，检查是否安全停止或明确提示，不发生任务重叠串流；
- [ ] 界面完全遵循 Apple HIG 与 macOS Tahoe 风格：圆角白底卡片、微阴影、系统语义色彩与分层通透材质。

### 2. 紧凑双轴折线图与动态自适应缩放 (Tight Auto-Range)
- [ ] 启动压测或 Ping 监控，观察左轴（CPS / 连接数）与右轴（延迟 ms）是否紧凑贴合实际数据波动区间，杜绝大面积留白；
- [ ] 曲线绘制平滑，支持成功线、丢包标记与失败断点；
- [ ] 数据点上限自适应控制（2400 点截断），内存占用平稳。

### 3. 图表手势操作：平移与鼠标滚轮缩放 (Zoom & Pan)
- [ ] 鼠标悬停在图表区域，使用鼠标滚轮上下滚动，测试横轴时间窗口的平滑放大（高精细观察）与缩小（全局轨迹浏览）；
- [ ] 按住鼠标左键横向拖拽，图表应平滑左右平移，松开后保持当前观察视窗；
- [ ] 点击“重置视图”或双击空白处，图表恢复实时跟随模式。

### 4. 悬浮 Tooltip 跟随光标移动与防溢出吸附
- [ ] 鼠标在折线图内移动，悬浮指标卡（时间戳、当前连接数、Ping 延迟、当前协议等）应紧密跟随鼠标指针；
- [ ] 当光标移至图表最右侧或最顶端时，Tooltip 自动智能翻转或向内偏移，严禁超出图表可视边界；
- [ ] 光标移出图表画布，Tooltip 立即渐隐消失。

### 5. 8 大核心指标仪表盘网格
- [ ] 右侧核心数据看板按 4 行 2 列网格呈现：
  1. 当前连接 (Active)
  2. 峰值连接 (Peak)
  3. 实际 CPS (Real-time Rate)
  4. 当前 Ping (RTT)
  5. 延迟抖动 (Jitter)
  6. 丢包率 (Loss Rate)
  7. 成功总数 (Success Target)
  8. 失败异常 (Failures / Aborts)
- [ ] 各指标数值动态跳动流畅，字体采用系统定宽/数字无衬线字体。

### 6. 连接释放动画与防 CC/WAF 拦截提示
- [ ] 点击“停止”后，若未勾选“保留连接”，观察释放进度条是否平滑推进；
- [ ] 模拟压测遭遇目标服务器限流或防 CC/WAF 阻断（大量连接在握手阶段被快速 RESET/拒绝），检查主界面是否弹出“检测到可能存在防火墙/WAF/Anti-DDoS 拦截”的诊断建议提示。

### 7. 桌面端检查更新与版本弹窗 (DesktopUpdateDialog)
- [ ] 点击标题栏右侧或关于界面的“检查更新”按钮；
- [ ] 若检测到新版本（或当前版本信息），弹出 Material 3 规范的 `DesktopUpdateDialog` 弹窗；
- [ ] 弹窗内完整展示版本号、构建号、发布日期与 Markdown 格式更新日志；
- [ ] 支持点击“前往 GitHub Release 下载”直接调用系统默认浏览器打开对应发布页。

### 8. 全平台默认失败上限调整
- [ ] 检查 Android、Desktop、iOS 各端默认失败上限（failureLimit）均统一设置为 `200`，避免在突发网络抖动时过早熔断测试。

---

## 历史核心功能回归重点

### 1. iOS 端原生接入验证
- [ ] 原生 `UITabBarController` 承载“测试、工具、历史、设置”；
- [ ] iOS 26+ SDK 编译下展现系统级 Liquid Glass 材质；
- [ ] 切换标签或退至后台时，网络探测立即停止，待机唤醒后无幽灵连接；
- [ ] NSLookup、RFC5780 NAT、Traceroute、MTU、iPerf3 各工具正常工作。

### 2. Android 端高并发打流验证
- [ ] 设置目标 CPS 2000~5000，验证发射窗口扩展与平滑发包；
- [ ] 验证 LogStore 日志写入优化后无掉帧，历史记录 7 天清理机制正常工作；
- [ ] 运行日志清空二次确认与 10 秒撤销功能完好。
