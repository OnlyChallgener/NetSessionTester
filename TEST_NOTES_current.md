# TEST NOTES - build85

重点验证：

1. 设置 100 CPS 时，平均发起速度应接近 100/s，不应再跑到 200+。
2. 设置 800 / 1000 CPS 时，实际 CPS 应按 1 秒快照显示，不卡顿闪动。
3. 活动数、失败数、实际 CPS 只应按 1 秒更新。
4. IPv6 / 高容量 IPv4 到 32360 附近应显示 Android FD / Socket 上限保护并释放。
5. 低容量 IPv4 应按低容量快停或分段失败阈值收尾。
