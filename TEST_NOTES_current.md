V1.1.2-internal build112 自测重点：
1. 网络信息刷新不再主动跑 NAT 类型测试，NAT 显示未测试/上次结果。
2. 点击 NAT诊断，选择 RFC5780，默认服务器为 stunserver2025.stunprotocol.org:3478。
3. 切换 RFC3489，默认服务器为 stun.voip.aebc.com:3478。
4. 服务器列表可添加、删除，至少保留一个。
5. NAT 结果显示端口保持/端口变化、开放/地址受限/端口受限等直白文案。
