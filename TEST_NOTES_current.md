# V1.1.15 build129 自测重点

1. 设置 Ping 间隔 25ms，观察信息栏是否显示 `瞬时xx.x/s` 和 `平均xx.x/s`。
2. 目标开放 TCP 端口时，信息栏应显示类似 `TCP:80` 或 `TCP:443`。
3. 测试刚开始时，X 轴应显示 0–15s 或 0–30s，不应直接固定到 0–1m。
4. 测试超过 1 分钟后，图表应显示最新 1 分钟窗口。
5. 拖动图表后进入历史查看，双击回到实时。
6. 观察 25ms 高频模式是否比 build128 更快进入稳定速率。
