# NetSessionTester v0.9.9-test90 自测包

用途：专门验证 build87/88/89 在 9000-10000 会话附近卡住的问题。

核心变化：
- versionCode = 90
- versionName = v0.9.9-test90
- 不发布，update.json 仍保持 build88，避免更新检测 404
- TCP 核心从流水线 pending 模型回退为批次高速模型
- 目标 CPS 字段在本自测包中作为每 100ms 的高速批量窗口使用，优先验证峰值能力
- 6000-12000：360 为软阈值，连续两批增长很弱才释放
- 6000-12000：600 为硬阈值
- 12000+：600 为硬阈值
- FD 保护线：32360
- 继续取消失败曲线、CPS曲线、3s/5s无增长检测

重点观察：
1. 能否突破 12000。
2. 如果仍卡 9000-10000，看失败原因是否主要为“超时/端口耗尽/FD上限”。
3. 如果能突破，说明之前是流水线 pending 模型导致的卡点。
