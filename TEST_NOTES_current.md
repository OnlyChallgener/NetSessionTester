# v0.9.9 build86 测试重点

1. 从 build84 / build85 安装后应能检测到 build86。
2. 更新弹窗不应再显示 vv0.9.9。
3. 更新弹窗不再显示“稍后更新”。
4. 点击一键更新后关闭下载弹窗，顶部应显示下载横幅。
5. 下载完成后顶部横幅点击应进入安装。
6. 默认失败兜底应为 600，旧版 1200 默认值应迁移为 600。
7. 会话数低于/等于 6000 且有失败时，应显示失败数小曲线。
8. 会话数超过 6000 时，不显示失败曲线，仅显示失败区间文字。
