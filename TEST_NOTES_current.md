# v0.9.9 build 81 自测说明

重点验证：

1. 手动 CPS 800/1000 是否比 build80 更贴近设定值。
2. 高容量 IPv6 是否不再中途大幅掉速僵直十几秒。
3. 失败数是否按分段阈值停止：<1000=120，<6000=200，<12000=360，>=12000=600。
4. TCP 超时固定 3000ms。
5. FD 接近时直接停止释放，不再慢速确认。

说明：本版没有改 update.json，不用于推送。
