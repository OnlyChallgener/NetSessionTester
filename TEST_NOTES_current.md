
## Build124 Ping 图表自测

- 25/50/100ms：自动高频，图形按聚合波形显示。
- 200ms 以上：自动低频，图形按普通折线显示。
- 丢包不连成普通延迟线，使用红色标记或区块。
- 支持左右拖动图形查看历史，双击回实时。
- 点击曲线显示当时数据或聚合区间统计。

## V1.1.15 build122 发布前自测重点

1. 应用信息版本显示 V1.1.15，versionCode 122。
2. 首页网络信息刷新后，“延迟”不应长期显示“不可用”；正常网络下应显示 ms。
3. NAT 类型仍保持“待检测”，需要手动点击 NAT 诊断，不要被首页刷新误触发。
4. 断网、GitHub 不可达、ICMP 被拦截时，APP 不应闪退；延迟可回落为“不可用”。
5. 底部设置/测试/历史按钮、增长曲线/峰值、历史筛选按钮点击时不出现方形阴影。
6. MTU 页面保留 综合 / ICMP / TCP / PMTU。
7. 漫游测试 Ping表 / 信号表轴数字不被圆角遮挡，漫游历史可展开/删除。

## build121 自测重点
- 点击底部 设置 / 测试 / 历史，观察是否还有方形阴影或方形水波纹。
- 点击测试页 IPv4 / IPv6、增长曲线 / 峰值按钮。
- 点击历史页 今天 / 昨天 / 本周、显示 10 / 30 / 100 条按钮。
- 这些按钮应只有圆角选中背景变化，不再出现矩形扩散。


# Build119 MTU 自测重点

- 综合模式应同时显示本地 MTU、TCP 业务、应用层 PLPMTUD说明、ICMP路径结果。
- 本地模式不应发网络探测包，只显示本机链路 MTU。
- TCP模式应验证 443 连接，并说明当前为 Java 层估算 MSS，NDK TCP_MAXSEG后续增强。
- ICMP模式应继续显示二分探测过程。
- PLPMTUD模式应提示需要专用测试节点，不应伪造结果。

# V1.1.14 build118 自测重点

1. 漫游测试非 WiFi 状态下点击开始，应提示“请连接WIFI”。
2. 漫游测试运行时，网络事件区域应显示 ConnectivityManager 监听到的网络可用/能力变化/链路属性变化。
3. Ping 表和信号表的 X/Y 轴数字应比 build117 更清楚，且不被圆角遮挡。
4. Ping 表 / 信号表竖线应对应 BSSID 切换事件。
5. 点击曲线应显示对应时间点的采样信息。
6. 停止测试后应自动生成总结，并保存到漫游历史。
7. 点击“漫游历史”应弹出历史卡片，最多保留 10 条，默认折叠，展开后显示延迟、RSSI、速率、漫游切换和网络事件摘要。
8. 测试标准 1s、高频 500ms、低频 2s 采样切换。
9. 继续观察小按钮/卡片点击水波纹和阴影是否还有方形扩散。


## V1.1.14 build120 自测
- 修复漫游测试 Ping表/信号表 X/Y 轴数字被圆角区域遮挡：增加 Canvas 高度、安全边距，X 轴按文字宽度居中并自动避让。
- MTU 检测去掉 PLPMTUD、本地，模式改为 综合 / ICMP / TCP / PMTU。
- 常用网络信息入口卡片、NAT 卡片、返回按钮、模式小按钮改为无方形 ripple/阴影扩散。

## Build122 编译修复补充
- 修复 Kotlin Regex 非法转义。
- WiFi 漫游事件文案改为网络事件/能力变化/链路变化，并显示具体含义。


## V1.1.15 build125 selftest
- Ping 统计区改为单行横向滑动，并新增抖动/时长。
- 抖动使用最近 50 个成功 RTT 的相邻差平均算法，丢包/超时不入队。
- Ping 图表信息栏支持横向滑动，去掉“延迟波形/自动高频”冗余文案。
- 新一轮 Ping 测试清空旧图表状态，停止后不再显示实时中。
- Ping 历史标题栏支持横向滑动并展示时长。
