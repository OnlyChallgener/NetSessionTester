# v0.9.9 build87 测试重点

1. 安装 build86 后检测更新，应提示 build87；安装 build87 后不应反复提示。
2. 更新弹窗版本显示应为 v0.9.9 build87，不应出现 vv0.9.9。
3. 会话图表只显示蓝色会话数曲线，不再显示失败小曲线。
4. 低会话阶段不再因 3s/5s 无增长终止，只按失败分段阈值或 FD 保护释放。
5. 测试释放后 CPS 卡片应显示“平均CPS”，不应显示最后时刻 0/s。
6. Ping 图表应继续刷新。
