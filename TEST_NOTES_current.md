# build143 自测要点

1. 漫游测试选择“路由器+外网”，确认丢包卡片显示“内x%/外y%”。
2. 仅路由器模式确认显示“网关x%”；仅外网模式确认显示“外网x%”。
3. 修改粘连 AP 参数后开始测试，确认事件区记录参数。
4. 在弱信号区域、同 SSID 存在更强 AP 时，观察是否出现“疑似粘连 AP”卡片。
5. 权限不足或 Android 限制扫描时，候选 AP 可能为空，不应崩溃。
