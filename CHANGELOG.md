
## V1.1.15 build124 selftest

- Ping 图形统一为延迟波形/折线图。
- 间隔 <200ms 自动高频，>=200ms 自动低频。
- 高频 Ping 按 250ms 聚合绘制，统计与渲染解耦，降低闪烁。
- 丢包改为红点/红线/淡红区块，不参与普通延迟连线。
- 图表支持左右拖动查看历史、双击/按钮回实时、点击查看点数据。
- X/Y 轴标签动态控制数量，避免拥挤和遮挡。

## V1.1.15 build123 selftest

- 修复从网络信息小卡片二级页返回后，网络信息卡片折叠状态被重置的问题。
- 修复从 NSLookup / Tracket / MTU / 漫游测试返回主页后，设置页滚动位置跳回顶部的问题。
- 将网络信息展开状态提升到主 App 层并用 rememberSaveable 保存。
- 将设置页 LazyColumn 滚动状态提升到主 App 层并用 LazyListState.Saver 保存。

## V1.1.15 build122 release

- 发布版本号更新为 V1.1.15 / build122。
- 修复网络信息轻量刷新时“延迟”一直显示“不可用”的问题。
- 延迟检测从完整 NAT/STUN 诊断中拆出，首页刷新时单独执行轻量检测。
- 轻量延迟优先 ICMP 检测 223.5.5.5 / 119.29.29.29，失败后回退当前目标 TCP 连接延迟。
- 保留 build121 的底部导航、测试模式、历史筛选按钮圆角点击修复。
- 保留 MTU 综合/ICMP/TCP/PMTU 与 WiFi 漫游图表/历史功能。

## V1.1.14 build121 自测
- 修复底部导航“设置 / 测试 / 历史”按钮方形点击阴影，改为无方形 ripple 的圆角胶囊点击区。
- 修复测试页 IPv4 / IPv6 / 增长曲线 / 峰值 等切换按钮点击时的方形扩散。
- 修复历史页 今天 / 昨天 / 本周 以及 10 / 30 / 100 条筛选按钮点击时的方形扩散。
- 继续保留 build120 的图表轴数字安全边距、MTU 综合 / ICMP / TCP / PMTU 结构。


## V1.1.14 build119 selftest

- MTU 检测重构为四合一结构：本地 MTU、ICMP 路径、TCP 业务、应用层 PLPMTUD。
- 新增本地 MTU 读取，作为手机当前链路上限参考。
- 新增 TCP 业务检测结果卡，默认验证目标 443 端口连通并给出 MSS/MTU 估算说明。
- 保留 ICMP 二分路径探测过程。
- PLPMTUD 改为专用节点预留说明，避免把公共网站误判为 UDP/QUIC 应用层 MTU。


## V1.1.14 build118 - 自测

- 漫游测试加入 ConnectivityManager 网络事件辅助监听。
- 新增漫游历史弹窗，最多保存 10 条，默认折叠，点击展开，可删除。
- 停止漫游测试后自动保存历史摘要。
- Ping 表 / 信号表轴数字加大并增加安全边距。
- 图表说明明确竖线为 BSSID 漫游切换事件。
- 漫游测试增加标准1s / 高频500ms / 低频2s 采样快捷选项。
- 继续保留 MTU 互补检测、PMTU增强、快速估测和非 WiFi 提示。


## V1.1.14 build117 · 自测增强
- 修复小按钮/小卡片点击水波纹和长按阴影方形扩散问题，统一使用圆角 Surface/裁剪。
- MTU 检测增加“互补 / 快速估测 / PMTU增强”模式，互补模式优先 PMTU/DF 语义探测，失败时降级快速估测。
- 漫游测试在非 WiFi 状态下启动会提示“请连接WIFI”。
- 漫游测试外网默认目标改为 223.5.5.5，避免死磕单一网站。
- 漫游测试拆分 Ping表 和 信号表，增加 X/Y 轴数字、动态刻度和点击曲线显示采样详情。
- 漫游测试自动生成 BSSID 切换详情卡片，停止后汇总延迟、RSSI、协商速率、漫游次数和切换附近丢包。

# V1.1.14 build115

- 发布版本号更新为 V1.1.14 / build115，versionCode 115。
- 首页 NSLookup / Tracket 入口卡片改为与网络信息小卡一致的字号和尺寸：58dp 最小高度、9dp 内边距、MarkBox 图标、11sp 小标题、14sp 主文字。
- 保留 V1.1.14 build114 下载横幅可关闭修复、HTTP 404 说明优化、NSLookup 自定义 DNS、Tracket 默认 30 跳等功能。

# V1.1.14 build114

- 发布版本号更新为 V1.1.14 / build114。
- NSLookup 增加 DNS1 / DNS2 自定义解析服务器，默认分别为 223.5.5.5 和 2400:3200::1。
- DNS1 / DNS2 均支持 IPv4 / IPv6 地址；每个输入框保存最近 3 条，支持下拉选择和单条删除。
- 本机 DNS 只用于固定展示，不参与自定义解析，不写入 NSLookup 查询历史。
- NSLookup 解析历史最多保存 15 条，点击复制完整结果，左滑单条删除。
- Tracket 默认跳数调整为 30，追踪过程实时显示，追踪历史最多保存 15 条。
- 首页 NSLookup / Tracket 入口文字放大，修复入口小卡片文字遮挡。

# Changelog

## V1.1.3 build113 hotfix

- 同版本紧急修复：NAT 手动诊断的 STUN 服务器列表现在会持久化保存。
- 修复退出 App 后 RFC5780 / RFC3489 自定义服务器恢复默认的问题。
- 自定义服务器会自动规范化为 host:port，不填写端口时默认 3478。
- 版本号保持 V1.1.3 / build113 不变。

## V1.1.3 build113

- NAT 手动诊断支持多个 STUN 服务器自动顺延。
- NAT 检测过程显示当前服务器和测试阶段。
- 继续收紧 RFC3489 / RFC5780 状态机。

## V1.1.3 same-version hotfix - net tools
- 网络信息卡片新增 NSLookup / Tracket 两个并排小入口卡片。
- 新增 NSLookup 二级页：读取本机 DNS、解析 A/AAAA、保存最近 10 条记录、左滑删除。
- 新增 Tracket 二级页：TTL Ping 方式追踪目标 IP 路由、保存最近 10 条分析记录、左滑删除。
- 设置卡片拖动排序加入 Lazy item placement 动画，降低换位闪跳感。

## V1.1.3 build113 same-version refine - net tools UI
- 仅修复首页 NSLookup / Tracket 两个入口小卡片文字遮挡，不改网络信息其它卡片。
- NSLookup 页面改为配置页风格：本机 DNS 固定显示，解析记录上限改为 15 条，记录卡片点击复制、左滑小按钮删除。
- Tracket 页面改为追踪配置风格：支持 IP 策略、跳数、超时配置，追踪过程实时显示。
- “分析记录/分析历史”统一改为“追踪历史”，历史上限 15 条，缩略显示，右上角展开后支持文本选择复制。
- 左滑删除按钮缩小为浅红小胶囊样式，减少占位。

## V1.1.14 build114 hotfix - download banner dismiss
- 修复更新下载失败横幅无法右滑/关闭的问题。
- 失败/完成状态横幅新增右侧关闭按钮，支持横向滑动关闭。
- 下载失败 HTTP 404 文案增加 GitHub Release 包未发布或 APK 文件名不一致提示。
- 修正 NSLookup / Tracket 首页入口字号，避免入口文字过大挤压。

### V1.1.14 build114 hotfix - shortcut font visual correction
- 再次下调首页 NSLookup / Tracket 两个入口卡片字号、图标和高度，修复视觉上仍偏大的问题。
- 版本号保持 V1.1.14 / build114 不变。

## V1.1.14 build116 self-test
- 新增网络信息固定入口：MTU检测、漫游测试。
- MTU检测支持 IPv4/IPv6/自动策略，使用二分法显示探测过程、结果和分析；IPv6 模式自动使用 AAAA 解析。
- 漫游测试支持路由器/外网/路由器+外网目标，采集 RSSI、协商速率、BSSID、延迟、丢包，并用曲线显示过程。
- NSLookup 新增“本机DNS / 自定义DNS”解析方式开关；本机DNS模式走系统解析，自定义DNS模式走 DNS1/DNS2。
- 自定义DNS查询优化：DNS1 空结果、超时或失败时会继续尝试 DNS2。
- NAT类型小卡片支持点击进入 NAT 诊断。
- 修复长按/点击卡片时部分阴影或水波纹呈方形的问题，入口卡片和网络信息卡片裁剪为圆角。

注意：此包为自测包，不建议直接发布 Release。


## V1.1.14 build120 自测
- 修复漫游测试 Ping表/信号表 X/Y 轴数字被圆角区域遮挡：增加 Canvas 高度、安全边距，X 轴按文字宽度居中并自动避让。
- MTU 检测去掉 PLPMTUD、本地，模式改为 综合 / ICMP / TCP / PMTU。
- 常用网络信息入口卡片、NAT 卡片、返回按钮、模式小按钮改为无方形 ripple/阴影扩散。

## V1.1.15 build122 compile hotfix
- 修复 MainActivity.kt 1174 行 Regex 非法转义导致的 Kotlin 编译失败。
- 漫游测试 WiFi 事件区文案改为“网络事件 / 能力变化 / 链路变化”，并显示具体含义。


## V1.1.15 build125 selftest
- Ping 统计区改为单行横向滑动，并新增抖动/时长。
- 抖动使用最近 50 个成功 RTT 的相邻差平均算法，丢包/超时不入队。
- Ping 图表信息栏支持横向滑动，去掉“延迟波形/自动高频”冗余文案。
- 新一轮 Ping 测试清空旧图表状态，停止后不再显示实时中。
- Ping 历史标题栏支持横向滑动并展示时长。


## V1.1.15 build126 selftest

- 测试控制改名为连接数测试。
- 连接数测试启动时自动重启一轮同步 Ping 监测。
- Ping 抖动加入图表信息栏靠前位置。
- Ping Y 轴增加 0-10 / 0-20ms 小延迟分档。
- Ping X 轴改为经过时间，去除负数显示。
- Ping 图默认最新 1 分钟滚动窗口，支持拖动回看和双击回实时。
- Ping 历史改为二级页面，最多保存 8 条，折叠展示，展开后显示图形和总结。
