# NetSessionTester V1.0.4-internal build104

自测版，不建议直接作为正式 Release 发布。

## 重点

- NAT 兼容口径优化：端口保持 + 多节点稳定时显示 NAT1 / 全锥形。
- 过滤行为主卡片显示结果，不再显示“多节点推断”。
- Ping 独立运行：可单独开始 / 停止。
- 新增 Ping 目标、间隔、次数、超时、协议设置。
- 新增 Ping 响应日志入口。
- 保留 V1.0.3 的定速发射核心、FD 32360 保护和释放耗时显示。
