## V1.1.16 build146

- 清理根目录冗余历史日志文件。
- 新增 docs/BUILD_HISTORY.md，统一记录历史 build。
- 新增 docs/RELEASE_TEMPLATE.md，统一后续发版模板。
- 设置 - 网络信息 - 路由追踪支持暂停/继续。
- 优化路由追踪停止、返回、后台、网络切换时的资源释放。
- 优化测试页、历史页、图表刷新性能，减少页面卡顿。
- 检查并优化稳定性、兼容性、权限和性能问题。
- 漫游事件从单一 AP 切换升级为 AP切换、频段切换、AP+频段切换、重新连接四类，并按 WiFi frequency 显示 2.4G / 5G / 6G / 未知。
- 漫游事件详情将旧“时长”拆分为“确认耗时”和“断流耗时”：确认耗时来自旧 BSSID 最后稳定采样到新 BSSID 首次稳定采样；断流耗时优先按网关 Ping 连续 timeout/loss 到恢复计算，外网 Ping 仅在未测网关时辅助。
- 漫游事件详情不再显示“切换附近丢包”，仅在检测到断流 timeout/loss 时显示“丢包x”；采样间隔不足以给出毫秒精度时显示“断流<采样间隔”或“断流未检测到”。
- 每次漫游事件增加前后 5 秒轻量分析，展示 RSSI、网关延迟和协商速率变化，并给出“切换有效 / 无明显收益 / 变差 / 需观察”的简短结论。
- 粘连判断增加频段维度：长期停留 2.4G 且弱信号、低速率、Ping 抖动或丢包时提示“疑似粘连频段”；无可靠扫描结果时不强行判断附近一定有更优 AP。
- 说明准确性边界：未 root Android 普通 APP 无法读取真实 802.11r/k/v 驱动级 roam complete 事件，确认耗时不等同于真实无线层漫游耗时。

# V1.1.15 build145

## 发版前清理与稳定性
- 根目录历史自测/发版说明已合并到 `docs/BUILD_HISTORY.md`，后续只保留 `CHANGELOG.md`、`TEST_NOTES_current.md` 和 `docs/` 下的长期文档。
- 新增 `docs/RELEASE_TEMPLATE.md`，用于后续发版前统一填写版本、权限、验证和风险项。
- `update.json` 更新到 build145，并改为正常 UTF-8 JSON 内容。

## 网络信息 / Tracket 路由追踪
- Tracket 追踪新增 Running / Paused / Finished / Failed / Canceled 状态。
- 运行中点击主按钮可暂停，暂停后按钮显示继续追踪，并保留已完成节点。
- 继续追踪从当前 TTL 进度往后执行，不清空已有结果。
- 停止、返回页面、网络不可用或 APP 进入后台时会取消追踪协程并销毁当前 ping 子进程。
- 暂停、继续、停止、完成、失败写入轻量事件行，避免高频刷屏。

## 兼容性与权限
- 未新增敏感权限；本次路由追踪继续使用已有 `INTERNET` 与 `ACCESS_NETWORK_STATE`。
- ping/traceroute 子进程运行在 `Dispatchers.IO`，UI 状态通过 Compose state 在页面协程中更新。

# V1.1.15 build144

## P0 必须项
- 内网丢包、外网丢包分开显示：实时区新增“内网丢包 / 外网丢包”，分别贴近“网关 / 外网”延迟显示。
- 丢包卡片替换为“粘连AP”卡片：实时顶部不再放单独丢包卡，改为显示“正常 / 疑似x段”。
- 稳定 / 标准 / 漫游三种模式替换旧频率：稳定 2000ms、标准 1000ms、漫游 500ms。
- 三种模式自动匹配 Ping 超时：稳定 1500ms、标准 1000ms、漫游 800ms；用户改动超时后进入自定义，支持一键恢复自动。
- Ping 图表继续使用自适应 Y 轴，并细化漫游 Ping 表档位，避免 100ms 左右直接拉到 200ms。
- 连接数测试明确使用折线趋势图，保留峰值/最高值视图用于截图对比。

## P1 建议项
- 新增漫游质量评分：综合丢包、Ping抖动、RSSI抖动、AP切换丢包、粘连AP和最高延迟，输出优秀/正常/一般/较差。
- 粘连AP判断参数保留：弱信号阈值、候选AP优势差、持续时间。
- 粘连AP判断加入近期丢包、Ping抖动、RSSI抖动因素，减少单靠RSSI误判。
- 漫游信号图 AP 切换线改为淡色虚线，从 X 轴连接到切换后 RSSI 端点，不再上下超出端点。
- 实时卡片更紧凑：RSSI、速率、粘连AP、质量一行；网关、内网丢包、外网、外网丢包一行。

## build144 热修修正
- 链路变化日志中 MTU 只有大于 0 才显示，避免出现“MTU 0”。
- 漫游 Ping 表 Y 轴档位细化为 20/30/40/50/60/80/100/120/150/180/200...，减少图表上半区空白。
- AP 切换竖线改为淡色虚线，从 X 轴连接到切换后 RSSI 端点，并在端点加小圆点。

## 说明
- Android 的候选 AP 扫描受系统权限、定位/WiFi权限和扫描频率限制影响，粘连AP仍属于“疑似诊断”。
- 本包未包含 Gradle Wrapper，当前环境无法直接编译 APK；请在 Android Studio 或 GitHub Actions 中构建验证。
