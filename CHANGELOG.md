# Changelog

## v0.9.9 build 73
- 回归高速 CPS 测量逻辑：用户修改新增值后优先按用户 CPS 执行，仅在失败率、FD 或资源风险时干预。
- 保留动态 FD 保护、释放图形进度、更新重试、公共 STUN 增强版 NAT 检测。
- 优化收尾释放：批量关闭 Socket 前先渲染释放状态，减少停止瞬间卡顿。
- 优化 IPv4 -> IPv6 切换：IPv4 释放后立即更新状态并缩短切换等待，减少切换时 UI 卡顿。
- CPS 曲线继续保持连续显示。
